{
  "ignoreCommand": "[ \"$VERCEL_GIT_COMMIT_REF\" = \"develop\" ] && exit 1 || exit 0",
  "installCommand": "npm install --legacy-peer-deps",
  "crons": [
    {
      "path": "/api/cron/email-drip",
      "schedule": "0 10 * * *"
    }
  ]
}
