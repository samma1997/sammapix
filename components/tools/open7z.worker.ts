/**
 * open7z.worker.ts
 *
 * DEPRECATED — questo file e mantenuto solo come stub per non rompere
 * eventuali import esistenti. La logica di estrazione .7z e stata migrata
 * direttamente in Open7zClient.tsx usando libarchive.js, che gestisce
 * il proprio worker interno (libarchive-worker.js in public/).
 *
 * Il file puo essere eliminato in una pulizia futura una volta verificato
 * che nessun altro modulo lo importa.
 */

export {};
